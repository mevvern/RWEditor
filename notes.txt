rendercontext should start out with 30 layers, where each layer contains levelsize.x * levelsize.y tiles...

each Tile layer has a container above it to hold the tile preview or props for that layer
rendercontext should have render modes that decide which group of the texture data to show, and which of those Tile layers to render
-geometry: 
